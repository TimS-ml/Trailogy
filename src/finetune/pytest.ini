[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra -q -m "not gpu"
markers =
    gpu: requires a CUDA GPU + bf16 + ~5 GB VRAM (Gemma 4 E2B load)
filterwarnings =
    ignore::DeprecationWarning
