data:
  train_split: train
  val_split: val
  image_size: 392
  num_workers: 12

model:
  backbone: vit_base_patch14_reg4_dinov2.lvd142m
  pretrained: true
  freeze_backbone: false
  drop_path_rate: 0.1

train:
  output_dir: outputs/vit-baseline
  num_train_epochs: 15
  per_device_train_batch_size: 64
  per_device_eval_batch_size: 96
  learning_rate: 3.0e-4
  backbone_learning_rate: 3.0e-5
  weight_decay: 0.05
  warmup_epochs: 1.0
  label_smoothing: 0.1
  dtype: bfloat16
  report_to: none
